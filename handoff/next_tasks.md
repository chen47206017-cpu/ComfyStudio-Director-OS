# 下一步

1. 保留已完成 Wan smoke 的证据，不自动重试；只有在新的 1080x1920 生产参数、字幕素材和明确授权都具备时才创建新任务。
2. 等用户确认 H3 下载 PowerShell 返回提示符后，再执行一次受控完整性与节点识别检查。`2026-09-03` 截图中的约 `4,278,120,438` 字节只是用户报告的进行中进度，不能替代最终大小或 SHA-256 检查。
3. 等用户填写 AutoDL Token 并明确授权后，才进行预算受限的云端 dry-run 之外真实测试；`CLOUD_5090_QUALITY` 仅是展示预设，不是 GPU 预留证明。
