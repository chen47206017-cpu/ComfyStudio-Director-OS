# Sample Asset Layout

This directory contains safe request examples only. It does not duplicate the
user's supplied image, driving video, or audio.

- three_images: character A, character B, and scene/layout inputs
- two_images_one_video: two character inputs and one driving video
- audio: dialogue and ambience input names

Place real files in the configured allowed media roots and update their paths
in a local request copy.

